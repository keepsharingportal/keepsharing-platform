// Family Resource Guide — uses the shared public page layout
// Navigation and footer are included within GuideDetailPage
export default function FamilyResourceGuideLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
