import { AdInventoryAdmin } from '@/components/advertise/AdInventoryAdmin'

export const metadata = { title: 'Ad Inventory — KeepSharing Admin' }

export default function AdInventoryPage() {
  return <AdInventoryAdmin />
}
