import { MarketingSystemClient } from '@/components/marketing/MarketingSystemClient'

export const metadata = { title: 'Marketing System — KeepSharing Admin' }

export default function MarketingSystemPage() {
  return <MarketingSystemClient />
}
